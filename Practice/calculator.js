var Userinput1= prompt("Enter the 1st number")
var Userinput2= prompt("Enter the 1st number")
var operator=prompt("enter the operation you want to perfrom from +,*,/,-")
if(operator=="+"){
    console.log(Number (Userinput1)+Number (Userinput2))
}
else if(operator=="-"){
    console.log(Number (Userinput1)-Number (Userinput2))
}
else if(operator=="*"){
    console.log(Number (Userinput1)*Number (Userinput2))
}
else if(operator=="/"){
    console.log(Number (Userinput1)/Number (Userinput2))
}